import { Box } from '@mui/material';
import React from 'react';
import Header from './Header';

const MenuItem = () => {
    return (
        <Box>   
          <Header/>
        </Box>
    );
};

export default MenuItem;